import React from 'react'
import { View, Text } from 'react-native'

const MenuDrawer = (props) => {
    return (
        <View>

            <Text style={{color: 'white'}}> Drawer Works! </Text>
        </View>
    )
}

export default MenuDrawer;
