import React from 'react'
import { View, Text } from 'react-native'

const Search = (props) => {
    return (
        <View style={{height: 650}}>
            <Text style={{color: 'white'}}> Search Works! </Text>
        </View>
    )
}

export default Search;
